import styled from 'styled-components';

const IconWrapper = styled.div`
  margin-top: 10px;
  display: flex;
  font-size: 14px;

  > span {
    margin-left: 15px;
    font-family: inherit;
  }
`;

export default IconWrapper;
